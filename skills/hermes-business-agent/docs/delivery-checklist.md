# 交付验收清单（Delivery Checklist）

> 打包完成后、交付给用户前，逐项确认（对应 SKILL.md 工作流第 ⑧ 步「交付」）。
> 机器可验的项由 `scripts/release_gate.py` 覆盖；带 ✍️ 的项为人工确认项。

---

## A. 机器门禁（release_gate 全自动）

- [ ] `python scripts/release_gate.py --verify-launch` 全绿（exit 0）
  - [ ] `quality_check`：py_compile + 结构门禁 + 离线桥接测试 + 源码签名漂移无破坏性变更
  - [ ] `check_endpoints`：前端→后端路由链路无未覆盖引用（运行时 404 隐患）
  - [ ] `check_js_modules`：前端 ES 模块强制校验（仅「禁用 HTMX/Pico 的原生 ES 模块前端」示例；无 node/无 JS 前端自动 SKIP，不阻塞纯 Python / Tkinter / HTMX·Pico 示例）；有 JS 前端时必须 SYNTAX-OK + ALL IMPORTS RESOLVED OK
  - [ ] `smoke_test_web`：网页无头冒烟，`GET /` 含关键 DOM id + `/healthz` 200（已把 B 档 DOM id 检查自动化）
  - [ ] `verify_launch`（`--verify-launch`）：06 §7 四步——解释器/依赖可解析 → 真起进程 → 端口 LISTENING + 健康端点 200 → 首页 200 且含 `--expect` 标志；收尾自动终止进程树并确认端口释放
  - [ ] 路线④⑤（`--api-server`）：`/health` 200 + `/v1/models` 认证存在（带 `--api-key` 则可列模型）；服务未起记 SKIP
  - [ ] CI 建议项 `verify_imports` / `check_refs`：仅告警不阻塞（失败也建议修）

## A2. 三系统架构（可选模式，仅当采用三系统分离时）

> 三系统分离是「单工程内嵌」的可选升级（见 `references/18-tristructure-architecture.md`）；未启用则本段不适用。

- [ ] `python scripts/verify_tristructure.py --root <三系统根>` 全绿（exit 0）：
  - [ ] `structure_ok`：业务系统/ + 连接系统/ + Agent系统/（或 替换Agent系统.md）骨架齐全
  - [ ] `biz_no_agent_import`：业务系统无 import 任何 Agent 模块
  - [ ] `conn_unique_assembly`：「from server import」仅存在于连接系统（唯一装配点）
  - [ ] `biz_independent_entry`：业务系统有独立入口（app.py + 启动.bat）
  - [ ] `agent_purity`：Agent系统（三系统根）无业务痕迹
  - [ ] `agent_zero_diff`（软）：底座工作区未被本地改动（打印 HEAD 短 sha 供比对上游）；不可判定 → SKIP，CI 用 `--strict` 计失败
  - [ ] `replace_doc`：`替换Agent系统.md` 在位（底座三步替换法）
- [ ] 融合模式：`连接系统/main.py` 启动后业务路由 + Agent 对话 `/api/chat` 双 200
      （`release_gate.py --verify-launch --launch-root 连接系统` 可代跑端口/健康/首页三段）
- [ ] 融合装配**真成功**：启动日志含「[连接系统] 融合装配完成」；出现「融合装配失败，回退业务自建 app」这行日志即不合格——
      此时页面照常 200 但里面没有 Agent 对话路由（禁止双 200 假绿的典型形态）
- [ ] 独立模式：`业务系统/启动.bat` 独立启动，业务路由 200（无 Agent 对话，符合预期）
- [ ] 底座替换：按 `替换Agent系统.md` 三步替换后，业务/连接零改动仍可融合装配

## B. 真实运行（✍️ 人工，最不能省）

- [ ] **启动 `启动.bat` 双击启动**，无需终端、无需额外配置
- [ ] 完成**一次真实 LLM 往返**：流式逐字可见 + 至少一次工具/推理事件出现
  - 高危：只测 HTTP 200 是假绿——Library 导入失败时 Web 框架照样 200
- [ ] 关键 DOM id 渲染正常（`smoke_test_web.py` 已按下述四项自动断言，这里作人工复核）：
  - [ ] `convSearch`（会话搜索框）
  - [ ] `usageChip`（用量芯片，位于 `composerFoot` 内）
  - [ ] `analyticsBody`（用量分析面板，若启用）
  - [ ] 首页标题含 `Hermes Desktop`
- [ ] 窗口关闭即退出（无残留进程）

## B2. 业务侧证据与发布基线（✍️ 人工确认，对应 SKILL.md 第 ①②⑤⑥⑦⑧ 步）

- [ ] 第 ① 步收料四类（业务规则 / 数据与接口 / 现有系统材料 / 业务人员口径答复）各有可指认的来源：一份文件、一个链接、或"某人某日的答复"三选一；缺的那类已登记成待办并写明向谁要；未经核对的原始材料留在暂存位，没有粘进系统提示词
- [ ] 第 ① 步契约已填齐六张表（面表 / 快照表 / 知识层清单 / 不变量表 / 字段契约表 / 本期目标与验收表），**每行能回指到一句业务事实或一段既有代码**；"未知"格已清零或转为已知待办
- [ ] 第 ① 步验收表**每一行的确认人与确认日期都不空**（`19` §7 第 7 张）；第 ⑦ 步跑的每条 Golden 用例都能回指到这张表的某一行
- [ ] **用例↔契约对账全绿**：`python scripts/check_golden_coverage.py --golden <用例集> --contract <契约表>` 在第 ⑤ 步每加一个工具后各跑一次、第 ⑦ 步开跑 Golden 回归前再跑一次，两次运行的 12 条硬门禁均无 FAIL（它只核覆盖与回指、不执行用例，因此不能代替第 ⑦ 步的回归通过率）
- [ ] 第 ② 步入口表已填齐（入口 → 场景 → 能力配置行 → 数据域，`19` §7 第 6 张）：每个入口的场景列能在面表指到真实一格，数据域列无空白与"同上"
- [ ] 第 ② 步第 5 条组织约束已成文：**回归半径**一句话（决定第 ⑦ 步跑哪些宿主原有功能用例）与**回退方式**一句话
      （制品 / 数据 / 配置三选一或并列，每种都有执行入口）；单机形态同样须有可执行的回退入口，不接受只写"重装上一版"
- [ ] 第 ② 步第 6 条组织约束已成文，三样各有一句话：**留痕的处所**（Issue / 工单 / 台账三选一，只有聊天记录不算）、
      **提交信息回指用的编号形状**、**一次评审最少要看到什么**（第 ⑤ 步：实现 / 用例 / 面表行 / 验收表回指四件 + 一句"本评审动了哪些用例的
      期望值、根据是验收表哪一行"；第 ⑥ 步动过审批与工具面配置的那一版：附当时的面 diff）
- [ ] **改过用例期望值的那些提交，逐条有签认记录**（第 ⑤ 步出口判据那一格）：对账门查得到有没有回指、查不到期望值本身对不对，
      这一格机检让路，只能由签认人认定；签认人取自 `19` §7 第 7 张对应行的确认人本人或其指定的人
- [ ] 第 ⑦ 步那次真实对话的**事件流已存档**（含 `stream_callback ≥ 1` 的证据、`action`/`action_result` 配对）
- [ ] **禁止面缺席断言通过**：`19` §6 的面 diff **按入口逐行**输出（禁止只交一份合并总数，那会把"cron 那条走默认全量面"摊平成 0），
      每个入口的禁止面工具都根本不在列表（禁止以"调用了但被拦住"充数）；配置表 loader 的 grep 命中文件全在一个模块内（`19` §6 第 6 条）
- [ ] 受控写面：每个写工具都有「审批被拒」与「重试前先读状态」两条用例，且**执行后回读值与结论一致**
- [ ] **跨会话越权负例**：换一个 `session_id` 重跑同一问题，返回的数据范围随之改变（不变 = 范围来自客户端或提示词，`15` §4ter）
- [ ] 写侧作用域已显式决定：要么设了 `HERMES_WRITE_SAFE_ROOT`，要么换了非 `local` terminal 后端；
      禁止把内置写黑名单当安全边界（源码自述不是边界）
- [ ] 业务 Golden 回归分维度通过率已记录：只读面 / 受控写面 / 越权负例 / 降级 / **多源冲突** / **写后回读不一致**（禁止只看总通过率）
- [ ] 验收跑在待交付制品上，不是源码工作副本：第 ⑦ 步 Golden 里**本期验收表覆盖到的那几条**已在制品上重跑一遍；
      `release_gate.py --verify-launch` 四步全绿只到「起得来、界面打得开」，不覆盖「业务做得完」（`06` §7 收尾）；
      冻结特有的三类缺陷（hidden-import 缺失、根路径钉错、资源文件没进包）只在制品里现身，源码上跑多少遍都补不出来（`06` §1–§2）
- [ ] 重跑在与生产隔开的环境：数据用副本（脱敏账套或专用测试账套）、`HERMES_HOME` 指向验收专用目录、模型 key 用验收专用的那一个；
      受控写面必须真写过一次并回读（第 ⑥ 步出口判据那两条与第 ⑦ 步「写后回读不一致」负例，不真写都验不到），
      禁止在生产数据上演示写；选了路线②③④ 的复用 `06` §9.8 的预发环境，禁止为验收另建第四套环境；
      上面那条换 `session_id` 的越权负例，只有副本与生产同构时结论才搬得动
- [ ] 验收环境与生产的差异逐格登记：模型与 key 的来源、副本的生成时间与数据范围、权限表来源、
      terminal 后端与 `HERMES_WRITE_SAFE_ROOT` 取值；未登记的差异会让换环境报错无法归因，下次发版的回归半径也定不出来
- [ ] 第 ① 步验收表每一行都指得出一次**制品上**的演示记录（谁看的、哪一天、看到什么）并记在本清单，
      同时登记**所验制品的指纹**（单文件：可执行文件的 SHA-256，或版本号 + 打包时间两格；常驻服务：`06` §9.6 的
      `releases/<ver>/` 版本号与当时的当前指向）——只写版本号认不出是同一次打包，回滚与线上归因都以这一格为锚；
      这一趟可由该行的确认人本人执行并签名：第 ① 步要求确认来自业务方而非开发自查，业务方在制品上亲眼跑过这一行是更强的确认
- [ ] 这一轮验收不过时的作废已按改动落点处理：改源码 → 制品重打、演示记录与指纹全部重取；改验收表那一行或改用例期望 →
      回第 ① 步重取确认人背书（`19` §7 第 7 张第三条）；只改环境配置 → 差异登记表补一行并写明重跑结果。
      禁止「改完顺手重跑一次就算过」——重跑的那一份已经不是被验过的那一份
- [ ] **六条发布基线**已随本次交付存档（下次发版才有"退化"可言）：Golden 分维度通过率、典型任务估算成本（标"估算"）、
      首字与整轮延迟 p50/p95、实际开放工具面宽与每轮工具面 token、审批批准率与回滚率、门禁结果
      （口径见 `references/21-operations-and-degradation.md` §6）
- [ ] **观察窗四格已写定并随交付说明与六条基线存在同一处**（SKILL.md 第 ⑧ 步）：
      ① 放量批次（先开给哪些入口 / 角色 / 账套；路线②④ 的「备用端口新实例」切流量本身就是放量；
       单文件自用形态换成「先在一台机器上跑满一个业务周期再分发」，不许留空当作不适用）；
      ② 窗口时长（下限是覆盖一次完整业务周期，不是统一一周）与窗内看哪几条基线 + 失败分类日志；
      ③ 回滚触发判据（写面回读不一致 / 越权命中 / 审批回滚率 > 0 / 成本与延迟劣化幅度，每条都要能由窗内数据判定）——
       事先写死的理由是出事时会先争「这算不算严重」，回退的时机就在争论里过去；
      ④ 收口方式（无触发即本期结项并归档；有触发按 `20` §6 归因回第 ⑦ 步，口径本身变了就回第 ① 步重取背书，重发后指纹与演示记录重取）
- [ ] 触发回滚后那条动作确实能单独执行：第 ② 步写的回退入口 + `06` §9.6（服务形态）或换一次可执行文件（单文件形态）
- [ ] **窗收口那一页「本期停在哪里」四格已写**（SKILL.md 第 ⑧ 步末拍；这一条到观察窗收口时才勾得到，交付前勾不上）：
      ① 验收表逐行有终态（已验 / 推到下期 / 主动不做，三种都要有落点，空白行不算）；② 本期主动划出的边界连同理由，
      每格回指到一句业务事实或第 ② 步某条约束（回指不到的那一格正是当时没想清、最该留给后来人看的）；③ 没清完的已逐条落成第 ② 步第 6 条约定那一处的带号单；
      ④ `21` §7 那几条运营节奏逐条写了认领人或"无人"（写"无人"是诚实的状态，空着等于默认它会发生；
      这三条没人跑，本清单上面的用例集就不再回归）
- [ ] 收口页与六条基线、最后一次的制品指纹与演示记录**归档在同一处**（同一个 `releases/<ver>/` 或同一份交付说明附录）；
      这一页只供后来人回看，没有写成"下一期必须从这里出发"那类强制

## B3. 服务侧交付（可选模式，仅当第 ② 步选了路线②③④；对应 `references/06-packaging.md` §9）

> 单文件桌面交付（路线①⑤）本段不适用。本段抓的是"起得来但守不住"这一类只在服务端形态下出现的问题。

- [ ] **未用冻结 EXE 当服务**：源码 + 专用 venv 常驻，`HERMES_HOME` 已重定向到数据盘独立目录（禁止数据落在制品目录）
- [ ] 守护层已配（WinSW/NSSM 或 systemd），崩溃后自动重启；**首启预检不过即带原因退出**，不静默重试
- [ ] 日志已重定向到文件并轮转；日志里**无明文 key / 凭据 / 未脱敏业务快照**（`21` §2.3）
- [ ] `/health`（存活）与 `/health/detailed`（就绪）分别被使用；守护层用「就绪」决定重启还是继续等
- [ ] 配置与密钥在制品之外（每 profile 一份 `.env`，权限收到服务账号）；多 profile 未共用同一个 key
- [ ] **重启自恢复**：手动 kill 服务进程 → 守护拉起 → `/health/detailed` 回到就绪
- [ ] **回滚演练**：预发环境走完 `06` §9.6 六步，并**实际执行过一次回滚**（制品回滚与数据回滚可分别执行）
- [ ] **多客户端不串**：两客户端各带自己的 `X-Hermes-Session-Id` 并发对话，历史与数据范围互不渗入；并发上限已设且拒绝响应确定
- [ ] 绑非本机地址前，`terminal` 已按 `15` §5 收进沙箱；`python scripts/release_gate.py --verify-launch --api-server --api-key …` 全绿
- [ ] `15` §10「方式 A 常驻服务的额外三条」全部勾选

## C. 产物与文档

- [ ] `dist/*.exe` 为 `--onefile` 单文件（无 `_internal/` 目录）
- [ ] `.hermes_data` 运行数据写入 `<exe>/hermes_data`，用户目录未被污染
- [ ] `README.md` 双用途（用户说明书 + LLM 克隆说明书）齐全：启动方式、API Key 配置、能力清单、已知限制
- [ ] 若接了自定义工具 / toolset：已在 README 写明白名单与 `disabled_toolsets` 策略

## D. 版本与漂移（若改了技能本体）

- [ ] `scripts/track_upstream.py`：**不带 `--gate`** 跑一次，读四条漂移线（① PyPI 版本 / ② 文档 md5 / ③ 源码签名 / ④ API 参考），任一 DRIFT 退出码为 1。带 `--gate`（即发布门禁里的 [0] 步）时四条全部降级为提示、恒返回 0——基线锁定版本是有意选择，不阻塞发布
- [ ] `scripts/check_api_signature.py`：OK:true（无 REMOVED/ADDED/DEFAULT_CHANGED）。**签名漂移真正的阻断点是这一条**（它同时是 `quality_check.py` 六段之一）
- [ ] 基线表 / `scripts/api-baseline.json` / `CHANGELOG.md` 已随版本更新
- [ ] `version` 号已 +0.1.0（见 SKILL.md _frontmatter）

---

## 反复核实（万无一失）循环

技能内容（API 签名 / 版本基线 / 路径引用 / 门禁脚本）**任何改动**后，必须重跑：

```bash
python scripts/track_upstream.py     # 上游漂移
python scripts/check_api_signature.py # 源码签名
python scripts/check_skill_gate.py    # 技能结构
python scripts/quality_check.py       # 全量门禁
python scripts/check_endpoints.py     # 路由链路
python scripts/check_js_modules.py    # 前端 ES 模块强制校验（原生 ES 模块前端示例；否则 SKIP）
python scripts/smoke_test_web.py      # 网页无头冒烟（结构级）
python scripts/release_gate.py        # 统一发布门禁（串联以上 + 2 CI 建议项）
```

**核心断言全绿才算落地**：上面每条脚本必须 exit 0。唯一例外是 `track_upstream.py --gate`——它对四条漂移线一律返回 0（见 D 节），所以"有没有漂移"要看它**不带 `--gate`** 那次的退出码，而签名漂移的硬阻断由 `check_api_signature.py`（在 `quality_check.py` 内）承担。任一项硬失败即阻断，先修再交付。
